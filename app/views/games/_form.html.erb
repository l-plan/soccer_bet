<%= form_with(model: game) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= form.text_field :nr %>
    <%= form.datetime_select :date %>
  </div>

    


  <div class="field">
    <%= form.collection_select :home_id , Team.order(:name), :id, :name%>
    <%= form.collection_select :away_id , Team.order(:name), :id, :name%>


    <%= form.text_field :score_home %>

    <%= form.text_field :score_away %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
