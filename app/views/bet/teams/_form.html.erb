<%= form_with(model: bet_team) do |form| %>
  <% if bet_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bet_team.errors.count, "error") %> prohibited this bet_team from being saved:</h2>

      <ul>
        <% bet_team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :participant_id %>
    <%= form.number_field :participant_id %>
  </div>

  <div class="field">
    <%= form.label :stage %>
    <%= form.number_field :stage %>
  </div>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.number_field :team_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
